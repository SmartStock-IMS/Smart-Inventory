{
  "name": "trolis",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "build": "npm run build --prefix client && npm run build --prefix server"
  },
  "author": "SensusHub (PVT) LTD",
  "license": "ISC",
  "description": "Web system for Trolis Cosmetics",
  "devDependencies": {
    "prisma": "^6.16.1"
  },
  "dependencies": {
    "@prisma/client": "^6.16.1",
    "bcrypt": "^6.0.0"
  }
}
